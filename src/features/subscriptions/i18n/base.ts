export default interface SubscriptionsTranslation {
  plans: {
    disclaimer: string;
    links: {
      why: string;
      costs: string;
    };
  };
  checkout: {
    payNow: string;
  };
  thankyou: {
    title: string;
    message: string;
  };
}
